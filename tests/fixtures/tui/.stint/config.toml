[project]
name = "tui-fixture"
default_sprint = "s1"

[gh]
repo = ""

[claim]
run = "claim-agent --id {id} --path {path} --area {area} --tags {tags}"

[[command]]
key = "z"
name = "Agent on task"
run = "agent --id {id} --slug {slug} --path {path} --title {title} --sprint {sprint} --estimate {estimate}"
claim = true
